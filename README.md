After running `npm install` (which you only need to do one time), to start your app, run the command `npm run dev`

Make sure to update the MongoDB string in server.js to start# YunChen-Chang-project3
